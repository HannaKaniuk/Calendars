import { Routes } from '@angular/router';
import { CustomersComponent } from './customers/customers.component'; // Убедитесь, что путь верный

export const routes: Routes = [
  { path: 'customers', component: CustomersComponent },
  
];